// export const MODEL_OPTIONS = [
//   { id: "gpt-4.1", label: "GPT-4.1" },
//   { id: "gpt-5.1", label: "GPT-5.1" },
// ] as const;

// export type AIModel = (typeof MODEL_OPTIONS)[number]["id"];

// export const DEFAULT_MODEL: AIModel = "gpt-4.1";

// export function isValidModel(model: string): model is AIModel {
//   return MODEL_OPTIONS.some((option) => option.id === model);
// }
