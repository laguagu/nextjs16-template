/**
 * Drizzle Kit configuration
 *
 * Uncomment and configure when you need database migrations.
 *
 * Example:
 * import { defineConfig } from "drizzle-kit";
 *
 * export default defineConfig({
 *   schema: "./lib/db/schema.ts",
 *   out: "./db/migrations",
 *   dialect: "postgresql",
 *   dbCredentials: {
 *     url: process.env.DATABASE_URL!,
 *   },
 * });
 */

export default {};
