/**
 * Shared TypeScript types for AI functionality
 *
 * Re-export types from models for convenience
 */

// Re-export model types
// export type { AIModel } from "../models/registry";

// Add additional shared AI types here as needed
// export interface AIResponse {
//   content: string;
//   model: AIModel;
//   timestamp: Date;
// }
